import { Plugin, PluginOptions } from './interfaces/plugin';

/**
 *
 * create a new instance of the plugin
 *
 */
export function createPlugin(options: PluginOptions): Plugin {
  const plugin: Plugin = {};

  return plugin;
}
